streamlit>=1.30.0
pandas>=2.0.0
Pillow>=10.0.0
# messages.parse() (structured outputs) requires the 1.x SDK; the previous
# >=0.18.0 floor allowed a 0.x release where that method does not exist.
anthropic>=1.3.0,<2
pydantic>=2.0.0
# Builds the .apkg deck package. AnkiDroid registers a handler for .apkg but
# not for text/csv, so this is what makes a phone export importable with one tap.
genanki>=0.13.0,<1
